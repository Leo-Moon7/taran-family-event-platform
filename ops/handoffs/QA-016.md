# QA-016 전달문 — 승인된 정보 나눔 홈 링크와 marketplace 검사 정합화

현재 상태는 `BLOCKED`다. CHG-A 테스트 정본과 소유권을 총괄 PM이 확정하기 전 실행하지 않는다.

## 작업 목적

FE-009에서 승인된 홈 `정보 나눔` 진입점을 유지하면서, 업체 목록·상세·비교·문의의 핵심 흐름에 커뮤니티 링크가 잘못 들어오는 것은 계속 막도록 자동 검사를 정합화한다.

## 읽어야 할 문서

- `AGENTS.md`
- `ops/TASK_SPECS.md`의 QA-016
- `ops/reports/FE-009-safe-beige-home.md`
- `ops/handoffs/FE-009.md`
- `ops/ACTIVE_WORK.md`의 CHG-A
- `scripts/tests/marketplace-flow.mjs`

## 수정 허용 경로

- `scripts/tests/marketplace-flow.mjs`
- `ops/reports/QA-016-marketplace-home-nav-contract.md`

## 수정 금지 경로

- `index.html`을 포함한 모든 제품 코드
- `package.json`, `pnpm-lock.yaml`
- `_verify/browser-smoke.cjs`, `scripts/tests/browser-smoke.cjs`
- API, DB, 라우팅, 환경변수
- CHG-B~C

## 구현 범위

- 변경 전 단일 실패를 재현한다.
- `index.html`에서는 승인된 `community.html` 링크가 존재하는지 긍정 검사한다.
- `venues.html`, `provider.html`, `compare.html`, `inquiry.html`에서는 기존 community 링크 금지를 유지한다.
- 리워드 금지와 그 밖의 marketplace 검사는 약화하지 않는다.
- 전체 테스트 결과와 제품 파일 무변경을 보고서에 기록한다.

범위 밖 문제는 직접 수정하지 말고 파일·행·재현 방법과 영향을 총괄 PM에게 보고한다. 테스트를 통과시키기 위해 제품 링크를 삭제하거나 정규식을 무조건 완화하지 않는다.

## 완료 조건

- 홈 정보 나눔 링크는 허용되고 존재가 검사된다.
- 나머지 4개 마켓플레이스 화면 금지는 유지된다.
- `pnpm test`, `pnpm build`, `pnpm test:dist`, `git diff --check`가 통과한다.
- 제품 파일 변경은 0건이다.

## 테스트 방법

- `node scripts/tests/marketplace-flow.mjs`
- `pnpm test`
- `pnpm build`
- `pnpm test:dist`
- `git diff --check`

## 총괄 PM 보고 형식

```text
작업 ID: QA-016
결과:
수정 파일:
변경 전 실패 재현:
변경한 테스트 계약:
보존한 금지 검사:
전체 테스트 결과:
제품 파일 변경 여부:
범위 위반 여부:
남은 문제·변경 요청:
롤백 방법:
```
