# 일일 프로젝트 점검 — 2026-07-21

## 요약

- 기능형 정적 프런트엔드+Supabase 계약+Netlify 배포 구조가 존재한다.
- 저장소 검증과 브라우저 스모크는 현재 작업 트리에서 모두 통과했다.
- 제품 코드는 이번 PM 작업에서 수정하지 않았다.
- 기존 미커밋 FE/QA 변경과 미추적 `docs/`, `favicon.ico`가 존재한다.
- Git 무시 `backend/**`에는 별도 SQLite/Python 수집 MVP가 있어 현 Supabase 기준선과 격리해야 한다.

## 실행 결과

| 검사 | 결과 |
| --- | --- |
| `pnpm test` | 통과: JavaScript 82, HTML 39, 보안/운영, 마켓플레이스, 8개 행사 개편 |
| `pnpm build` | 통과: `dist/` 생성, Supabase 설정 없음 경고 |
| `pnpm test:dist` | 통과: HTML 39, 로컬 참조·공개 제외 목록 |
| `pnpm test:browser` | 통과: 390/768/1440px, 홈→목록→상세 |

## 작업 트리

- 수정: `package.json`, `pnpm-lock.yaml`, `provider.html`, `scripts/pages/provider.js`, `scripts/pages/venues.js`, `styles/components/filter.css`
- 미추적: `docs/**`, `favicon.ico`, `scripts/tests/browser-smoke.cjs`, 이번 PM 운영 문서
- 무시된 로컬 영역: `backend/**`, `_verify/**`, `dist/**`, `node_modules/**`

## 충돌·차단

- CHG-A와 CHG-B가 동일 작업 트리에 섞여 있어 추가 작업 전 소유권·범위 검토 필요.
- 브라우저 스모크가 `_verify/`와 `scripts/tests/`에 두 벌 존재하며 내용도 달라 CHG-A 검토에서 정본 선택 필요.
- D-01~D-03 전 검색 공개 로직 변경 금지.
- 스테이징 정보 전 운영 백엔드·보안 완료 판정 금지.
- ADR-008 선행 조건 전 `backend/**` 수집 스크립트 실행 금지.

## 오늘 실행 가능한 다음 작업

1. QA-002 정적 업체 데이터 품질 기준선 재현
2. MKT-001 SEO·콘텐츠 공백 기준선 작성

두 작업은 제품 코드를 수정하지 않고 정확한 결과 파일도 겹치지 않는다. 첫 검토 지시에 따라 자동 시작하지 않았다.
