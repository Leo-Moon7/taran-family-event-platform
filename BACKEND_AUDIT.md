# 백엔드 연결 감사

## 단일 운영 구조

- 인증: Supabase Auth
- 데이터베이스: Supabase PostgreSQL
- 파일: 비공개 Supabase Storage
- 권한: RLS + `taran_admin_profiles`
- 호스팅: Netlify

공개 페이지와 관리자는 동일한 Supabase REST·Auth·Storage 구조를 사용합니다. `/api` 호환 호출은 `scripts/core/auth.js` 내부에서 동일한 Supabase 데이터로 변환하며 별도 서버와 혼합하지 않습니다.

현재 로컬 격리 통합 후보에는 `migrations/001`~`016`이 모두 있습니다. 운영 DB 적용, Edge Function 배포, 런타임 활성화, 실제 자료 접수는 각각 별도 승인 단계입니다.

## 연결된 기능

- 회원가입·로그인·로그아웃·계정 삭제 요청
- 관심 업체, 계산기, 체크리스트 계정 저장
- 견적 문의
- 내부 후기 등록과 공개 검수
- 견적·사진 공유, 포인트, 리워드 교환
- 업체 담당자 권한 요청, 비공개 서류 검수, 승인 업체 전용 수정 RPC
- 커뮤니티 글·댓글과 글 공개 검수
- 업체·준비백과·배너·회원·통계 관리자 CRUD
- v2 업체 자료·과거 견적 구조와 운영 검토 큐 계약. 실제 접수·증빙 업로드·공개 반영은 비활성

## 보안 확인

- service role key와 DB 비밀번호는 저장소·브라우저 코드에 없습니다.
- 사용자 입력은 `textContent` 또는 구조화된 필드로 출력합니다.
- 비공개 파일은 공개 URL을 만들지 않고 권한 확인 후 서명 URL을 발급합니다.
- 파일 MIME, 용량, 저장 경로, RLS를 제한합니다.
- 관리자 쓰기는 역할별 RLS를 통과해야 합니다.
- `015`의 런타임 플래그 4개는 기본값 `false`이며 v2 Storage 버킷·정책·업로드 URL을 만들지 않습니다.
- `016`의 운영 검토 큐는 `owner`·`admin`·`operations`의 AAL2 세션만 허용하고, 6개 비민감 필드를 한 번에 최대 50건만 반환합니다.
- v2 기본 테이블은 브라우저 역할에 직접 열지 않고 명시된 RPC 경계만 사용합니다.

## 운영 전 외부 확인

통합 완료 후 새 프로젝트는 `admin-schema.sql`을 한 번 실행한 뒤 `003`~`016`, 기존 프로젝트는 적용 이력과 백업을 확인한 뒤 누락된 가장 이른 번호부터 `016`까지 적용해야 합니다. 실행 전 한 체크아웃에 모든 대상 SQL이 실제로 있는지 확인합니다. 첫 owner 계정 등록과 Netlify의 `SUPABASE_URL`·`SUPABASE_ANON_KEY` 설정도 필요합니다.

운영 적용 전에는 격리 환경에서 `015`의 비활성 기본값·Storage 부재·직접 권한 차단과 `016`의 AAL2·역할·최소 필드·페이지 제한을 재검증합니다. 실제 업체 자료 접수, 증빙 업로드, 공개 반영, 운영 DB 변경은 사용자 승인 전 실행하지 않습니다.
